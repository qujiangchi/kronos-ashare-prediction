# Kronos 徐工机械（000425）实战指南
### 一份经 31 窗口回测验证的「怎么用 / 怎么别用」操作手册

> 适用对象：把 Kronos 金融 K 线基础模型用于 A 股个股 30 日预测的人。
> 验证标本：徐工机械 000425，数据区间 2024-01 ~ 2026-07，31 个历史 30 日窗口 walk-forward 回测。
> 注：徐工是「验证标本」。你实盘持有的是 560860（工业有色ETF）、513160（港股科技ETF）这类腾讯敞口标的；
> 要把本方法用到任何持仓上，**必须按第 5 节先对该标的重跑校准 + 回测**，k/h 因股而异，不能套用。

---

## 0. 先给结论（最重要的一段）

| 指标 | 实测值 | 含义 |
|---|---|---|
| 方向命中率 h | **61%**（95%CI ≈ 44%~78%） | 只比抛硬币（50%）高一点点，**统计上不可区分** |
| 幅度系数 k | **0.026** | 模型涨跌幅 ≈ 真实的 1/38，**本质是噪声** |
| 端点误差 | 未校准 13.1% / 校准 8.6% | 压平后反而更贴近真实（徐工波动小、常均值回归） |

**一句话：Kronos 不是徐工的「预测器」，而是「趋势延续假设生成器」。它自身没有可交易的 Edge。**

- 方向 ≈ 赌运气，不能单独下注。
- 幅度 = 噪声，永远不能当价格目标。
- 回测里被压到接近 0 的版本反而最准——说明模型 ±20% 的摆动是幻觉，压掉才对。

---

## 1. 那它到底能干嘛（唯一正确的用法）

把 Kronos 当成一个**独立的趋势外推视角**，只做三件事：

1. **一致性校验**：模型方向与你的独立判断 / 均线 / 动量**同向** → 增强信心；**反向** → 当作「模型在延续旧趋势、可能错过拐点」的提醒（不是反向信号，只是警示）。
2. **夸张度警报**：模型未校准若给出 >±10%/月 的幅度，而现实日波动仅 ~2%，说明它在过度外推 → 直接忽略幅度数字。
3. **绝不单独据此开仓。**

---

## 2. 实战决策框架（每天照做）

每日收到校准邮件后，按这条链走：

```
① 看「模型方向」(偏强/偏弱) 和 「k/h 口径」
        ↓
② 问：方向与你的周/月均线趋势同向吗？(独立判断)
        ├─ 同向 → 模型作「佐证」，信心 +；但仍只按你原有计划执行
        └─ 反向 → 标记「模型外推惯性，警惕拐点」，不改变你的计划
        ↓
③ 若你本就打算交易，开仓前三重确认：
        ✓ 模型方向与均线/动量同向
        ✓ 不与已知基本面催化背离
        ✓ 单笔仓位 ≤ 你风控上限（见第 4 节）
        ↓
④ 严格止损/止盈纪律，不因为「模型说涨」而扛单
```

**关键纪律：Kronos 的输出永远只是「弱先验」，权重必须低于你的其他分析。它的价值是「多一个视角」，不是「决策依据」。**

---

## 3. 当前快照（徐工机械，2026-08-25）

- 当前价 **8.02**；近期真实走势：**20日 −10.9%、60日 −17.1%**（下行通道）。
- 模型本次外推：**偏强**（未校准 +23.5% / 校准 +0.6%）。
- **按本框架判读**：模型方向与近期真实下行**背离** → 这正是第 1 节说的「模型延续旧形态、可能错过拐点」场景。
- **结论：不据此做多；把它视为旧趋势惯性外推，警惕反转。幅度数字（无论 ±多少）一律不看。**

---

## 4. 风控底线（不可省略）

- 单笔仓位 ≤ 总资产 **___%**（请你按风险偏好自填，试仓建议 ≤5%）。
- 任何单日预测涨跌幅 > ±10%（A股涨跌停）一律当模型噪声。
- 预设止损（如 −8% 离场），到点执行，不扛。
- Kronos 在徐工上无统计 Edge；若坚持用它的信号，亏损概率与不用的差异不显著——**它帮不了你赚钱，但用错方式（信幅度、单独下注）会害你。**

---

## 5. 换成你的持仓（560860 / 513160）前必做

本指南的 k=0.026 / h=61% **只对徐工有效**。要用于其他标的：

1. 跑 `calibrate_xugong.py` 的同类脚本（改代码里的 `sz000425` 为目标代码），得到该标的的 k、h。
2. 跑 `backtest_uncal_vs_actual.py` 同类脚本，确认端点误差与方向命中率。
3. 只有当某标的 **h 显著 > 55% 且 k 处于合理区间（如 0.3~0.8）** 时，才值得把模型当「弱信号」纳入框架；否则一律按徐工结论处理——只看方向作佐证，幅度全忽略。

---

## 6. 诚实边界

- ⚠️ 本指南基于徐工机械单标的 31 窗口回测；换标的必须重新校准 + 回测。
- ⚠️ 本结果**仅供研究 / 参考，不构成任何投资建议**。是否交易、交易什么、交易多少，由你自负盈亏决定。
- ⚠️ 框架不承诺盈利；它的作用只是「帮你别被模型的幅度幻觉带偏」。

# Kronos 预测系统 · 优化实验日志

> 目标：逐步优化 11 只自选股的 Kronos 校准预测系统。每步都必须：
> **假设 → 实验（带数据）→ 据数据决定下一步 → 提交并 push GitHub**。
> 本文件是唯一的进度账本：每步在此追加，夜间循环任务读取"下一步"继续执行。

## 方法论与约束
- 模型：Kronos-base（NeoQuasar）。本质是**趋势外推器**：顺着近期 K 线惯性延续，趋势延续时方向对、拐点处反转。
- 运行：`HF_ENDPOINT=https://hf-mirror.com D:/Creater/Kronos/venv/Scripts/python.exe <script>`
- 实验产物放 `optimization_output/`；新脚本命名 `exp_<step>_*.py`。**禁止修改** `predict_multi.py` / `build_email.py` / `calibrate_*.py` 核心逻辑，除非某步明确"采纳改动"且已验证。
- 每步结束：把结果（数据表 + 结论 + 下一步）追加到本文件，然后 `git add -A && git commit && git push`（push 若因网络失败可重试，不影响本地提交）。
- 提交格式：`exp(step N): <标题>`。

## 基线事实（已确认）
- 11 只票均已完成 walk-forward 月度锚点校准（31 锚点，但 `sample_count_calib=2`，样本极小）。
- 各票 k（收益压缩）/ h（30日方向命中率）：
  - 中国平安 0.21/64.5%、华天科技 0.24/61.3%、徐工 0.07/61.3%、泰瑞 0.38/54.8%、铜陵 0.17/54.8%、招商银行 0.20/51.6%、启明星辰 0.15/51.6%、今世缘 0.19/48.4%、源飞 0.00/41.9%、招商积余 0.12/38.7%、同仁堂 0.00/25.8%。
- 多数 h 在 26%–65%（贴近随机 50%）；k>0 仅表示方向净偏，幅度不可信。
- 回测结论：×k 压缩后端点/路径误差最低（vs 未校准/方向保留+约束），校准方向正确。
- 已知伪影：首日 level-shift（已用"首步=其余日中位"修复）；幅度夸大（已用 ×k 修复）。

## 假设 backlog（按优先级）
1. ~~【已完成】多 horizon 方向命中率 + 动量基线 + 显著性~~ → 见 STEP 1。结论：模型20/30日方向61.3%但不显著(p=0.281,n=31)；校准管线经交叉验证可复现。下一步=增大独立样本量检验显著性。
2. **【已完成·STEP 2】增大独立样本量重测显著性**：见 STEP 2。**结论：方向信号不可靠（n 增大后回归随机）**。
3. **【下一步·STEP 3】多票复刻显著性（检验"方向信号是否普适"）**：对 中国平安/华天科技/招商银行 用同法大样本重测，判定是否仅徐工失效还是 Kronos-base 在 A 股个股层面普遍无方向 alpha。见 STEP 3。
4. 聚合方式扫描（median vs mean，SC 4/8/16）。
5. 回看窗口 LB 扫描（100/150/200）。
6. 滚动/扩张校准窗口 vs 固定 2024-2026。
7. 可交易子集筛选（仅保留 h 显著 >50% 的票）。
8. Kronos 方向 vs 朴素动量 head-to-head（是否真有增量价值）。
9. 元模型：Kronos 方向 + 简单均线(MA20/60 金叉死叉) 组合。
10. 模型尺寸复测（small vs base）在单位算力下的性价比。

---

## STEP 1 — 多 horizon 方向命中率 + 动量基线 + 显著性
- **假设**：Kronos 作为趋势外推器，在较短 horizon（10–20 日）方向可能比 30 日更准（长期外推误差累积）；且可能并不优于"直接用近 20 日动量方向"这种朴素基线。
- **实验**：对徐工(000425) 用 2024-01~2026-07 月度锚点（同校准），LB=150，SC=8，记录模型在 H∈{5,10,15,20,30} 的预测方向命中率；同时算两个朴素基线——近20日动量方向、近5日动量方向——的同 horizon 命中率；并对每个 horizon 做二项检验（vs 50%，精确双尾 p）。
- **指标**：各 horizon 的 模型命中率 / 动量20命中率 / 动量5命中率 / p值；并交叉验证 30 日模型命中率是否≈校准 JSON 的 61.3%。
- **脚本**：`exp_step1_xugong_horizon.py`，产物 `optimization_output/step1_xugong_horizon.csv` + 控制台表。
- **状态**：✅ 完成（6m49s，31 锚点）。
- **结果（有效锚点 n=31）**：

  | H | 模型 | 动量20 | 动量5 | p(vs 50%) |
  |---|------|--------|-------|-----------|
  | 5  | 48.4% | 48.4% | 58.1% | 1.0000 |
  | 10 | 54.8% | 35.5% | 32.3% | 0.7201 |
  | 15 | 45.2% | 41.9% | 32.3% | 0.7201 |
  | 20 | **61.3%** | 51.6% | 41.9% | 0.2810 |
  | 30 | **61.3%** | 58.1% | 45.2% | 0.2810 |

- **交叉验证**：30日模型命中率=61.3% == 校准JSON的 0.613 → 校准管线可复现、可信（方法学验证通过）。
- **结论**：
  1. 模型方向信号在 **20/30 日最强（61.3%）**，且同时高于两个动量基线；在 H=10 模型 54.8% 也远高于动量(35.5%/32.3%)。说明 Kronos 比"裸动量"更稳——但动量基线对徐工本身很弱（短期动量仅 32-36%），"胜过动量"含金量有限。
  2. **核心缺陷**：n=31 下任一 horizon 的二项 p≥0.281，**无法拒绝"抛硬币+运气"**。95% 近似 CI 约 43%–78%，太宽。
  3. 因此**既不采纳、也不放弃**：证据不足源于样本太小，而非方向错误。
- **下一步（STEP 2）**：扩大**独立**样本量以检验显著性，而非换方向。月度锚点天然独立（相邻锚点测试窗不重叠），把历史拉到 ~2015 起可得 ~100+ 锚点；加 Wilson 95% CI。若 H=20/30 在 n≈100 时 p<0.05 → 采纳并进入"逐票选 horizon"(backlog 3)；若仍不显著 → 结论"方向信号不可靠"，转向幅度/波动率参考或元模型(backlog 9)。

---

## STEP 2 — 大样本(独立月度锚点)重测方向显著性
- **假设**：STEP 1 的 61.3% 方向命中率是真实的弱信号，只是被 n=31 掩盖；扩大到 ~150 个独立月度锚点后，H=20/30 的命中率应维持 ~60% 且二项 p<0.05（显著优于抛硬币）。
- **实验**：徐工(000425)，分块拉取全历史 2013~今（gtimg count 上限~1024，按 3 年分窗避免截断，去重合并 → 实测 3273 条），月度锚点（独立，≥150 前置日），LB=150，SC=4，H∈{5,10,15,20,30}；记录模型命中率 + 动量20/动量5 基线 + 二项精确 p + Wilson 95% CI。
- **指标**：各 horizon 模型命中率 / Wilson 95% CI / 动量基线 / p 值；重点看 H=20 与 H=30 是否 p<0.05 且 CI 下界 >50%。
- **脚本**：`exp_step2_xugong_largesample.py`，产物 `optimization_output/step2_xugong_largesample.csv` + 控制台表。
- **状态**：✅ 完成（17m9s，n=155 独立锚点）。
- **结果（n=155）**：

  | H | 模型 | Wilson 95% CI | 动量20 | 动量5 | p(vs 50%) |
  |---|------|---------------|--------|-------|-----------|
  | 5  | 54.8% | [47.0%, 62.5%] | 38.7% | 49.7% | 0.2607 |
  | 10 | 53.5% | [45.7%, 61.2%] | 43.9% | 49.7% | 0.4219 |
  | 15 | 45.8% | [38.2%, 53.7%] | 48.4% | 50.3% | 0.3351 |
  | 20 | **49.7%** | [41.9%, 57.5%] | 51.6% | 49.0% | **1.0000** |
  | 30 | 53.5% | [45.7%, 61.2%] | 47.7% | 48.4% | 0.4219 |

- **结论（决定性）**：
  1. 样本从 31→155 后，H=20/30 命中率从 61.3% **回落到 49.7%/53.5%**；**所有 horizon 的 95% CI 都跨过 50%，二项 p 全部 ≥0.26**（H=20 甚至 p=1.0）。STEP 1 的 61.3% 是**小样本噪声**，真实方向命中率 ≈ 抛硬币。
  2. 动量基线在 n 增大后也回到 ~44–52%（同样无信号）。徐工在这段历史里既无 Kronos 方向 alpha，也无动量 alpha。
  3. **Kronos-base 对徐工机械不提供可靠的方向信号。**
  4. **更重要的推论**：原校准体系中基于 `sample_count_calib=2` 算出的各票 `h`（平安64.5%、华天61.3%、招商51.6%…）**本质是噪声**，不能作为"方向命中率"使用。当前 `predict_multi.py` 依赖的 `direction_hit_rate` 校准项**不可信**，其"方向保留"逻辑应废弃或重构。
- **下一步（STEP 3）**：方向失效可能是徐工个体问题，也可能是 Kronos-base 在 A 股个股层面的普遍现象。需在其他票上复刻大样本检验以判定。直接复用已写好的 `exp_step3_multistock.py`（对 中国平安/华天科技/招商银行 跑同法）。
  - 若 ≥2/3 票在 H=20/30 显著(p<0.05) → 方向信号在部分票存在，保留这些票方向、逐票选 horizon。
  - 若 <2/3（尤其全不显著）→ 结论"Kronos-base 方向信号在 A 股个股层面普遍不可靠"，系统定位转向：**仅用 Kronos 做幅度/波动率参考（k 压缩仍可能有效），方向改用可证伪简单规则(MA 金叉/趋势)或元模型(backlog 9)**，并废弃 `h` 校准项。

---

## STEP 3 — 多票复刻方向显著性（检验普适性）
- **假设**：STEP 2 表明徐工方向信号是噪声。待检验：这是徐工个体问题，还是 Kronos-base 在 A 股个股上普遍无方向 alpha。若普遍无 alpha，则原"方向预测"定位必须重构。
- **实验**：复用 `exp_step3_multistock.py`，对 中国平安(sh601318)/华天科技(sz002185)/招商银行(sh600036) 用与 STEP 2 完全一致的大样本月度锚点法（LB=150, SC=4, H∈{5,10,15,20,30}），输出每票每 horizon 的命中率/Wilson CI/动量基线/二项 p。
- **指标**：每票 H=20/30 的命中率、Wilson 95% CI 下界、p 值；跨票汇总"显著票数 / 总票数"。
- **脚本**：`exp_step3_multistock.py`（已写），产物 `optimization_output/step3_multistock.csv` + 控制台摘要。
- **状态**：⏳ 待运行（由主 agent 或夜间自动化执行，预计 ~50min/3票）。
- **预期决策**：见 STEP 2 结论第 4 点分支。
